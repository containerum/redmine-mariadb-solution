
{
    "name": "{{.REDMINE_NAME}}-{{.HASH}}",
    "deploy": "{{.REDMINE_NAME}}-{{.HASH}}",
    "ports": [{
        "name": "redmine",
        "target_port": {{.REDMINE_PORT}},
        "protocol": "TCP"
        }]
}